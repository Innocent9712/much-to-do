apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
  namespace: muchtodo
  labels:
    app.kubernetes.io/name: mongodb
    app.kubernetes.io/component: database
    app.kubernetes.io/part-of: muchtodo-application
type: Opaque
# Note: In production, use sealed-secrets or external secret management
# Values are base64 encoded: echo -n 'value' | base64
data:
  # root (base64 encoded)
  MONGO_INITDB_ROOT_USERNAME: cm9vdA==
  # mongopassword123 (base64 encoded)
  MONGO_INITDB_ROOT_PASSWORD: bW9uZ29wYXNzd29yZDEyMw==
